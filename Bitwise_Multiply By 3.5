#include <stdio.h>

int main() {
int n;
    scanf("%d", &n);
    n= (n<<1) + n + (n>>1);
    //n = n*2 + n + n/2
    // n= (n<<2) - (n>>1);
    //n= n*2*2 - n/2
    // n=(n<<2)- (n>>1);
    printf("%d", n);
    return 0;
}
