#include <stdio.h>

int main()
{ int n;
  scanf("%d",&n);
 int garg=0;
 
 if(n&1)
 {
    garg=garg+1; 
 }
 
 if(n&2)
 {
    garg=garg+1;  
 }
 
 if(n&4)
 {
     garg=garg+1; 
 }
 
 if(n&8)
 
 {
     garg=garg+1; 
 }

 
 printf("%d",garg);
 
    return 0;
}
