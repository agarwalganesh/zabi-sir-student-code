#include <stdio.h>

int main() {
int n;
    scanf("%d",&n);
    if(n>0){
    printf("<");
    int term = 1;
    for(int i = 1; i<=n; ++i){
        
        printf("%d", term);
        if (i < n) { 
            printf(" ");
        }
        term *= 2; 
    }
    printf(">");}
    
    return 0;
}
